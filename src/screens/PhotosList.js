import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const PhotoList = () => {
    return(
        <View>
            <Text>Liste de photo</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container:{
        flex:1
    }
})

export default PhotoList